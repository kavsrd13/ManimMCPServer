# Minimal requirements for testing FastMCP deployment
# Use this if you need to verify the MCP server works without Manim rendering

fastmcp==0.2.0
fastapi==0.111.0
uvicorn==0.30.6
pydantic==2.8.2
typing_extensions>=4.9.0

# Manim and its dependencies commented out for minimal deployment
# These require system libraries (ffmpeg, cairo, pango, latex) that may not be available
# manim==0.18.1
# pycairo==1.26.0
